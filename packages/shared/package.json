{
  "name": "@panelmind/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and Zod schemas for PanelMind",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo \"(no shared lint yet)\" && exit 0",
    "test": "echo \"(no shared tests yet)\" && exit 0",
    "clean": "rm -rf dist .turbo"
  }
}
