# ─── Dependencies ───────────────────────────────────────────────
node_modules/
venv/
.venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd
.pnpm-store/

# ─── Build outputs ─────────────────────────────────────────────
.next/
out/
build/
dist/
target/
*.tsbuildinfo
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
coverage/
htmlcov/

# ─── Next.js ───────────────────────────────────────────────────
# (most already covered by .next/ above)

# ─── Environment ───────────────────────────────────────────────
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env
!.env.example

# ─── IDE / System ──────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db

# ─── Claude / AI tools ────────────────────────────────────────
.gemini/
.agents/
.agent/
_agents/
_agent/
tmp/

# ─── Scratch / planning artifacts ─────────────────────────────
implementation_plan/
token-usage-output.txt
backend/Untitled-1.txt

# ─── App-specific ─────────────────────────────────────────────
backend/db/pitch_memory.db
backend/data/*.db
backend/data/*.sqlite
backend/data/*.sqlite3
frontend/public/charts/
apps/api/data/*.db
apps/api/data/*.sqlite
apps/api/data/*.sqlite3

# ─── Tier 0 reorganization ───────────────────────────────────
# During the monorepo migration (Tier 0a → 0b → 0c), the existing
# backend/ and frontend/ directories are still where the code lives.
# They will be moved to apps/api/ and apps/web/ in a follow-up PR.
# The new monorepo tooling (apps/, packages/, turbo.json) is being
# introduced alongside, not replacing.
#
# Once the migration is complete, the lines below will be uncommented:
#   backend/
#   frontend/

# ─── Tier 0d placeholder: monorepo scratch ───────────────────
.turbo/
